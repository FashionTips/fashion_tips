<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns:beans="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns="http://www.springframework.org/schema/security"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-4.0.xsd">

    <http>
        <http-basic entry-point-ref="entryPoint"/>

        <!-- Permit OPTIONS request for all to support cors -->
        <intercept-url pattern="/**" method="OPTIONS" access="permitAll"/>

        <!-- Permit unauth access to images -->
        <intercept-url pattern="/images/**" method="GET" access="permitAll"/>

        <!--Permit posts GET request for unauthorized users.-->
        <intercept-url pattern="/posts/*" method="GET" access="permitAll"/>

        <!-- Only authenticated users can access the api methods -->
        <intercept-url pattern="/**" access="isAuthenticated()" />
        <csrf disabled="true"/>
        <custom-filter ref="restLogoutFilter" position="LOGOUT_FILTER"/>
    </http>

    <!--Initialize custom entry point for HTTP Basic Auth-->
    <beans:bean id="entryPoint" class="com.bionicuniversity.edu.fashiontips.security.CustomBasicAuthenticationEntryPoint">
        <beans:constructor-arg name="realmName" value="Fashion Tips Application"/>
    </beans:bean>

    <!-- Initialize custom logout filter for REST interface -->
    <beans:bean id="restLogoutFilter" class="com.bionicuniversity.edu.fashiontips.security.CustomLogoutFilter">
        <beans:constructor-arg>
            <beans:bean class="org.springframework.security.web.authentication.logout.HttpStatusReturningLogoutSuccessHandler" />
        </beans:constructor-arg>
        <beans:constructor-arg>
            <beans:list>
                <beans:bean class="org.springframework.security.web.authentication.logout.SecurityContextLogoutHandler"/>
            </beans:list>
        </beans:constructor-arg>
    </beans:bean>

    <authentication-manager>
        <authentication-provider>
            <password-encoder hash="bcrypt" />
            <jdbc-user-service
                    data-source-ref="dataSource"
                    users-by-username-query="select login, password, 1 from users where login = ?"
                    authorities-by-username-query="select login, role from users
                                                   join roles on users.id = roles.user_id
                                                   where login = ?"/>
        </authentication-provider>
    </authentication-manager>
</beans:beans>